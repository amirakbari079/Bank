package bankApp;

public class InvestmentAccount extends Account {
    public InvestmentAccount(String userName, String cardNumber, Integer accountBalance, String bankName ,String accountType) {
        super(userName, cardNumber, accountBalance,bankName, accountType);
    }
}
